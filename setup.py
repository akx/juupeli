import setuptools

if __name__ == "__main__":
    setuptools.setup(
        name="juupeli",
        author="Aarni Koskela",
        author_email="akx@iki.fi",
        version="0.1",
        packages=setuptools.find_packages(include=("juupeli",)),
    )
